interface OrbitRingProps {
  name: string
  description: string
  price: string
  radius: number
  angle: number
  scrollProgress: number
  opacity: number
  delay: number
}

export function OrbitRing({ 
  name, 
  description, 
  price, 
  radius, 
  angle, 
  scrollProgress,
  opacity, 
  delay 
}: OrbitRingProps) {
  return null
}
